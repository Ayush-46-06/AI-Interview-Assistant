import AppLayout from '../components/AppLayout'

// Full interview session UI is implemented in Phase 2.
// This page establishes the route and component boundary.
export default function InterviewSessionPage(): React.JSX.Element {
  return (
    <AppLayout>
      <div className="p-8 text-gray-400 text-sm">
        Interview session UI — Phase 2
      </div>
    </AppLayout>
  )
}
